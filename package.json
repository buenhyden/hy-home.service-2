{
  "name": "hy-home.service-2",
  "version": "0.1.0",
  "description": "입력받은 텍스트의 내용을 분석하고 이 내용을 바탕으로 이미지를 생성하며, 생성된 이미지를 바탕으로 뉴스를 브리핑하는 영상을 제작하도록 한다. ",
  "scripts": {
    "init": "pwsh ./scripts/bootstrap-new-project.ps1 || ./scripts/bootstrap-new-project.sh",
    "setup": "pwsh ./scripts/setup-workspace.ps1 || ./scripts/setup-workspace.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "template",
    "ai-agent",
    "standardization"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
