# ==============================================================================
# Docker Compose CI Template
#
# 목적 (Purpose):
# - CI/CD 환경 또는 로컬 테스트를 위한 서비스 구성
# - Database, Cache 등 의존성 서비스 실행
#
# 사용법 (Usage):
# - 필요한 서비스의 주석을 해제하여 사용하세요.
# - (Uncomment services as needed.)
# ==============================================================================

version: "3.8"

services:
  # ----------------------------------------------------------------------------
  # Application (Main App)
  # ----------------------------------------------------------------------------
  # app:
  #   build: .
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgresql://user:password@db:5432/myapp
  #     - REDIS_URL=redis://cache:6379/0
  #   depends_on:
  #     - db
  #     - cache

  # ----------------------------------------------------------------------------
  # Database (PostgreSQL / MySQL / etc.)
  # ----------------------------------------------------------------------------
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     - POSTGRES_USER=user
  #     - POSTGRES_PASSWORD=password
  #     - POSTGRES_DB=myapp
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U user"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  # ----------------------------------------------------------------------------
  # Cache (Redis / Valkey)
  # ----------------------------------------------------------------------------
  # cache:
  #   image: redis:7.2-alpine
  #   ports:
  #     - "6379:6379"
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres_data:
